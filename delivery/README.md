# FlashFit delivery app

Build from the repository root with `npm run build:delivery`. The active delivery source is this folder; the build writes a self-contained deployable output to `delivery/dist/`.
