# FlashFit customer app

Build from the repository root with `npm run build:customer`. The active customer source is this folder; the build writes a self-contained deployable output to `customer/dist/`.
