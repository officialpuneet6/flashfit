# Public configuration

Only environment name, Supabase URL, publishable key, public analytics IDs, and safe public feature configuration belong in browser builds. Server credentials and provider secrets remain in secure backend configuration.
